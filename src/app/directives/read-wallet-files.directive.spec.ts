import { ReadWalletFilesDirective } from './read-wallet-files.directive';

describe('ReadWalletFilesDirective', () => {
  it('should create an instance', () => {
    const directive = new ReadWalletFilesDirective();
    expect(directive).toBeTruthy();
  });
});
