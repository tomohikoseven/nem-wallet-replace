import { HashKeyvaluePipe } from './hash-keyvalue.pipe';

describe('HashKeyvaluePipe', () => {
  it('create an instance', () => {
    const pipe = new HashKeyvaluePipe();
    expect(pipe).toBeTruthy();
  });
});
